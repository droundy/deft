#!/bin/bash

# I use bask because I prefer its builtin time function over /usr/bin/time.

set -ev

sh autogen.sh

# First make sure there are no warnings...
CXXFLAGS='-ansi -pipe -W -Wall -Werror' ./configure
time make -j2 recheck
make clean

# And then make sure the thing compiles properly with the default flags.
./configure
time make -j2 check
