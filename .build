#!/bin/bash

# I use bash because I prefer its builtin time function over /usr/bin/time.

set -ev

./autogen.sh

# First make the code generator, so we can get started generating code
# soonest.
time make -j2 optimize-functionals

time make -j2 recheck
